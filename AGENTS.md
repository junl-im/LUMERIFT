# LUMERIFT 작업자 절대 규칙

이 파일은 사람·AI·자동화 작업자가 프로젝트를 이어받을 때 가장 먼저 읽는 진입점이다.

## 시작 전 필수 순서

1. `docs/MASTER_BIBLE.md`를 읽는다.
2. `docs/HANDOFF_MASTER.md`를 읽는다.
3. `HANDOFF_STATE.json`에서 현재 버전, 완료·진행·예정 상태를 확인한다.
4. `docs/HANDOFF_LOG.md`의 마지막 릴리스 기록을 확인한다.
5. 기존 기능·파일을 근거와 인수인계 기록 없이 임의로 삭제하거나 축소하지 않는다.

## 종료 전 필수 순서

모든 기능 패치와 에셋 패치는 아래 파일을 반드시 갱신한다.

- `HANDOFF_STATE.json`
- `docs/HANDOFF_MASTER.md`
- `docs/HANDOFF_LOG.md`
- `docs/CHANGELOG.md`
- `docs/ROADMAP.md`
- `RELEASE_MANIFEST.json`
- `README.md`

그리고 `npm run validate:handoff`와 전체 검증을 실행한다.

## 절대 고정 조건

- 게임명: **LUMERIFT: 균열의 계승자**
- Vite + TypeScript + PixiJS 8 + Firebase
- GitHub Pages 배포
- 모바일 웹 우선, 세로형 9:16, 2.5D 쿼터뷰
- PNG/WebP만 허용, SVG·런타임 SVG 생성 금지
- 기본 60FPS, 절전 30FPS
- 초기 다운로드 15MB 이하 목표
- Texture Atlas, Lazy Loading, Object Pool 우선
- 최종 상용 아트 교체 시에도 런타임 키와 경로 계약 유지
- MVP 완료 전 실시간 MMO·PvP·길드전·거래소·글로벌 채팅 추가 금지

## 릴리스 보고 형식

사용자에게 결과를 전달할 때 반드시 다음 순서를 지킨다.

1. 결과
2. 전체 통합 ZIP
3. 덮어쓰기용 패치 ZIP
4. 다음 업데이트 내용

## 패치 정책

- 전체 통합 ZIP은 신규 설치와 전체 교체용이다.
- 패치 ZIP은 사용자가 마지막으로 받은 직전 배포 버전 기준으로 제공한다. 필요 시 별도 누적 패치를 추가한다.
- 기준 버전에 덮어쓴 결과가 전체 통합본과 일치하는지 대조 검증한다.
- 삭제 대상은 `PATCH_DELETE_LIST.txt`에 기록한다.
- 패치 적용 결과는 전체 통합본과 파일 해시가 일치해야 한다.

## 문서 우선순위

충돌 시 우선순위는 다음과 같다.

1. `docs/MASTER_BIBLE.md`
2. `AGENTS.md`
3. `docs/HANDOFF_MASTER.md`
4. `HANDOFF_STATE.json`
5. 시스템별 세부 문서
6. 소스 코드 주석

## 에셋 단계 및 품질 진실성 규칙

에셋 규모·용량·프레임 수와 시각적 완성도는 별개다. 구조용 자산을 최종급으로 보고하지 않는다.

모든 신규 자산은 다음 중 하나로 표시한다.

- `prototype`: 임시 기능 검증
- `production-structure`: 경로·Atlas·성능 계약 검증
- `production-candidate-procedural`: 고해상도 절차형 제작 후보. 외부 아트 디렉션과 수작업 리터칭 전에는 final 또는 AAA라고 부르지 않는다.
- `production-candidate-open-art-pass`: 명시적 재배포 라이선스가 있는 실제 게임용 공개 원본을 기본 런타임에 적용한 단계. 독점 원화·최종 세계관 통일 전에는 final 또는 AAA라고 부르지 않는다.
- `production-candidate-unified-art-pass`: 공개 원본 기반 실사용 자산에 공통 명암·림라이트·팔레트·배경 단계 규칙을 적용한 단계. 독점 실루엣 원화가 아니므로 final 또는 AAA라고 부르지 않는다.
- `final-candidate`: 수작업 원화·리터칭·모바일 실기기 검수를 통과한 상용 후보
- `final-approved`: 아트 디렉터 승인, 저작권 확인, 성능 QA까지 완료한 최종 자산

### 필수 보고 항목

에셋 릴리스는 반드시 다음을 기록한다.

- 런타임 자산 용량과 원본 보관 자산 용량
- Atlas 수, 프레임 수, 애니메이션 수
- 해상도와 포맷
- 현재 품질 단계
- 최종 상용 원화 여부
- Lazy Loading 및 초기 15MB 예산 영향

`production-candidate-procedural` 이하 자산을 최종급으로 보고하지 않는다. 자동 생성 수량만으로 고품질·AAA·최종 완성을 주장하지 않는다.


## Vite 8 빌드 규칙

- `build.rollupOptions.output.manualChunks`는 반드시 함수 형식으로 작성한다.
- 객체 별칭 형식은 TS2769 재발 원인이므로 금지한다.
- CI는 Node.js 24 호환 GitHub Actions 메이저를 사용한다.

## 실사용 에셋 완료 판정

- 실제 로비·전투·인벤토리 등 런타임 화면에 기본 연결되어야 완료로 기록한다.
- 외부 에셋은 제작자, 라이선스, 원본 이름, 가공 범위를 `docs/THIRD_PARTY_ASSETS.md`와 기계 판독 JSON에 기록한다.
- 여러 출처를 혼합한 공개 에셋 패스는 독점 IP 아트 또는 최종 AAA 아트로 표현하지 않는다.
- 기본 경로에서 빠진 기존 절차형 자산은 레거시 보관소로만 유지한다.

## 배포 용량 및 표기 규칙

- 사용자 보고의 파일 용량은 십진 단위 `MB`로 표기한다. `MiB`는 기술 참고가 꼭 필요한 경우에만 병기한다.
- **전체 통합 ZIP에는 코드·문서·런타임 자산·모바일 제작용 원본·라이선스·레거시 보관 자산을 모두 포함한다.**
- 전체 통합 ZIP에서 원본이나 기존 자산을 근거 없이 삭제하지 않는다. 단, 모바일 제작 품질에 영향이 없는 과도한 해상도·비압축 원본은 문서화된 규격에 따라 최적화할 수 있다.
- 경량 실행본이 필요하면 `LUMERIFT_RUNTIME_...zip` 또는 `LUMERIFT_DEPLOY_...zip`으로 별도 제공하며 전체 통합본이라고 부르지 않는다.
- 초기 다운로드 15MB 기준은 최초 로드 번들에만 적용하며 전체 프로젝트·전체 자산 용량과 혼동하지 않는다.
- `art_source`는 전체 통합본에 보존하지만 `public` 밖에 두어 GitHub Pages 배포와 초기 다운로드에는 포함하지 않는다. LUMERIFT 소유 원본은 `docs/SOURCE_ART_POLICY.md`의 모바일 제작용 상한을 따른다.
- 레거시 제작 후보 자산은 전체 통합본에 보존하되 기본 `AssetCatalog`에서 참조하지 않는다.
- 모든 릴리스에서 `npm run validate:archive`로 원본·보관 자산 누락 여부를 검사한다.

## BAT 파일 정책

- `INSTALL_AND_START.bat`와 `VERIFY.bat`는 Windows 편의용 선택 도구다.
- npm 명령과 GitHub Actions가 공식 기준이며 BAT 파일이 없어도 빌드·검증·배포가 가능해야 한다.
- BAT 내부 버전·Node 요구 조건은 현재 릴리스와 일치시킨다.

## UI·그래픽 완료 판정

- 화면을 기능만 연결한 상태로 완료 처리하지 않는다.
- 로비·전투·인벤토리·스테이지·결과 등 주요 화면은 런타임 연결, 미리보기, 시각 감사 문서, 자동 검사까지 있어야 완료다.
- 대시보드형 텍스트 패널 반복, 동일 사각 버튼 반복, 서로 다른 화풍의 무분별한 혼합을 금지한다.
- 정보 우선순위, 터치 영역, Safe Area, 전투 가시 영역, 명도 대비를 검수한다.
- 공개 원본 색보정은 통일 패스일 뿐 독점 최종 원화 제작으로 보고하지 않는다.

## 운영 보상·저장 규칙

- 공지·출석·우편·쿠폰의 읽음·수령·사용 이력은 `PlayerProfile.operations`에 저장한다.
- 로컬 개발 모드에서도 동일 보상을 두 번 지급하지 않는다.
- 온라인 운영 전환 시 출석 날짜, 쿠폰 유효성, 우편 첨부 보상은 서버 시간과 Cloud Functions에서 재검증한다.
- 운영 화면 완료 판정에는 런타임 연결, 저장 마이그레이션, 중복 방지, 미리보기, 자동 검사가 모두 필요하다.

## Firebase 고정 규칙

- Web Config는 공개 식별자이므로 소스에 포함할 수 있지만 서비스 계정 JSON, Admin SDK 비공개 키, reCAPTCHA secret은 절대 커밋하지 않는다.
- Firebase 클라이언트 SDK는 npm modular API를 사용하고 CDN script 방식과 compat API를 혼용하지 않는다.
- 모든 Firestore 경로는 기본 거부이며 사용자 데이터는 `request.auth.uid`로 소유권을 확인한다.
- 쿠폰, 고가치 우편, 서버 출석 보상은 클라이언트 계산만으로 최종 승인하지 않는다.
- 규칙 변경 시 `npm run validate:firebase`와 Emulator 권한 테스트를 수행하고 인수인계 파일을 갱신한다.
- Spark 무료 할당량을 보호하기 위해 공지 캐시, 저장 쓰기 최소화, 무제한 실시간 리스너 금지를 유지한다.

## Firebase App Check 고정 결정

- Firebase App Check는 사용자의 명시적 재승인 전까지 SDK·reCAPTCHA 키·CI Secret·Console enforcement를 모두 비활성화한다.

## Cloud Save·랭킹 운영 규칙

- 자동 Cloud Save는 로컬 저장을 먼저 완료한 뒤 Firestore에 기록한다.
- 로컬과 클라우드의 `updatedAt` 차이가 의미 있게 발생하면 계정 화면에서 양쪽 요약을 보여주고 수동 방향 선택을 제공한다.
- 수동 다운로드는 클라우드 원본을 로컬에 적용하며, 수동 업로드는 로컬 원본을 클라우드에 적용한다. 버튼 문구에서 방향을 명확히 표시한다.
- 랭킹은 Spark 할당량 보호를 위해 무제한 실시간 리스너를 사용하지 않는다.
- 전체·주간 랭킹 쓰기는 본인 UID 문서만 허용하고 주간 문서 ID는 `{weekKey}_{uid}` 형식을 강제한다.
- 클라이언트 계산 점수는 경쟁 보상 지급 근거로 사용하지 않는다. 서버 권위 검증 전에는 표시용 MVP로만 취급한다.
- Rules 또는 Indexes 변경 시 `npm run firebase:test:rules` 실행 절차와 결과를 인수인계에 기록한다.

## public 에셋 수명주기 규칙

- `public/assets`에는 현재 `ASSET_MANIFEST.json` 또는 `AssetCatalog.ts`에서 도달 가능한 실사용·Lazy Loading 자산과 필수 라이선스만 둔다.
- 구버전, 미사용 지역, 품질 후보, 샘플, 대체 음원은 `art_source/runtime_archive/<release>/public/assets`에 원래 상대 경로를 보존하여 이동한다.
- 이동은 삭제로 처리하지 않으며 경로·바이트·SHA-256을 `asset_registry/RELOCATION_PLAN_<release>.json`에 기록한다.
- 보관 자산을 다시 사용할 때는 현재 아트 방향·해상도·라이선스를 재검수한 뒤 새 버전 런타임 경로와 매니페스트에 등록한다.
- 모든 에셋 릴리스는 `npm run validate:asset-cleanup`과 `npm run report:asset-cleanup`을 통과해야 한다.

## v1.8.1 PixiJS Graphics 고정 규칙

- PixiJS 8 `Graphics`에서 객체형 `lineStyle({ ... })` 호출을 사용하지 않는다.
- 가이드·윤곽 경로는 경로 작성 후 `stroke({ color, alpha, width })`로 마감한다.
- GitHub Actions 빌드 오류가 보고되면 전체 UI 작업보다 먼저 최소 핫픽스를 릴리스한다.

## v1.9.0 공통 UI 시스템 고정 규칙

- Boot·타이틀·로그인·로비와 일반 장면은 `UiSkin`, `UiTheme`, `UiButton`, `SceneChrome`을 우선 사용한다.
- UI 패널·버튼·탭·슬롯을 장면 내부에서 임의 색 사각형으로 새로 복제하지 않는다. 필요한 프레임은 v5 Atlas에 추가한다.
- 타이틀 디자인 시안과 문서 미리보기를 실제 런타임 캡처라고 보고하지 않는다.
- 기본 터치 영역은 44×44 CSS px 상당 이상을 유지하고 Safe Area·주소창·가상 키보드 영역을 침범하지 않는다.
- 교체된 UI는 삭제하지 않고 버전별 runtime_archive에 보존한다.


## v1.10 모바일 UI 고정 규칙

- 터치 가능한 핵심 행동은 시각 크기와 무관하게 최소 48×48 논리 픽셀 판정 영역을 제공한다.
- `visualViewport` 사용 시 height뿐 아니라 offsetTop·offsetLeft·scale을 함께 고려하며 키보드 높이를 중복 차감하지 않는다.
- 모바일 텍스트 입력은 16px 이상을 유지하고 Safe Area·가상 키보드에 가려지지 않아야 한다.
- 쿠폰과 같은 핵심 입력 흐름에 `window.prompt`를 사용하지 않는다.
- pointer cancel·outside·화면 회전·페이지 복귀 후 눌림 또는 입력 차단 상태가 남지 않아야 한다.
- reduced motion 사용자는 반복 펄스와 과도한 축소 애니메이션을 최소화한다.
- 개념 시안과 디자인 QA 합성 이미지를 실제 런타임 또는 물리 단말 캡처로 보고하지 않는다.
- 실제 단말을 직접 계측하지 않은 FPS·발열·GPU 메모리 결과를 완료로 기록하지 않는다.


## v1.10.1 성능·접근성·복구 JSON 고정 규칙

- 실제 온도 센서에 접근하지 않은 상태에서 프레임 추세 기반 `estimatedPressure`를 발열 측정값으로 표현하지 않는다.
- 자동 품질 하향은 여러 측정 구간의 1% Low·긴 프레임 저하를 확인한 뒤 적용하고, 상향에는 최소 60초 이상의 안정 구간을 요구한다.
- 색상만으로 HP·위험·보스 상태를 구분하지 않고 `♥`, `▲`, `◆` 또는 동등한 기호를 함께 제공한다.
- 접근성 개인 설정은 계정 진행 저장과 분리하며 Cloud Save 스키마를 임의 확장하지 않는다.
- 복구 JSON은 현재 로그인 UID와 일치할 때만 적용하고, 가져오기 전 현재 로컬 상태를 복구 지점으로 보존한다.
- JSON 가져오기는 로컬에 먼저 적용하며 Cloud Save 업로드는 사용자의 명시적 행동으로 남긴다.
- 플레이어 실루엣 블록아웃을 최종 런타임 원화·애니메이션 완료로 보고하지 않는다.



## v1.11 전투·그래픽 강화 고정 규칙

- Rift Drive·스타일 등급·Overdrive는 전투 피드백이며 서버 권위 랭킹 점수로 사용하지 않는다.
- 텔레그래프와 실제 타격은 계속 동일한 `AttackFootprint`를 사용한다.
- 적응형 렌더 예산은 시각 비용만 줄이고 충돌·피해·쿨다운·AI 판정을 바꾸지 않는다.
- 프로그램 하이라이트·그림자·곡선 효과를 독점 최종 원화 업그레이드로 과장하지 않는다.
- 신규 런타임 이미지를 추가할 때는 `ASSET_MANIFEST.json`, 용량 보고와 15MB 초기 예산을 함께 갱신한다.
- 실제 단말에서 측정하지 않은 장시간 FPS·온도·배터리·GPU 메모리 결과를 완료로 기록하지 않는다.

## 사용자 결과 보고 필수 형식

모든 개발·수정·복구 릴리스의 최종 답변은 예외 없이 아래 3개 번호와 순서로 제공한다.

1. `작업한 내역`: 실제 수정 파일·기능·검증 결과·미검증 항목을 기록한다.
2. `다운로드 파일`: 신규 설치·전체 교체용 통합 ZIP과 기존 프로젝트 최상위에 그대로 덮어쓸 수 있는 패치 ZIP을 모두 제공한다.
3. `다음 예정 내역`: 다음 버전에서 진행할 항목을 기록한다.

전체 통합 ZIP 또는 패치 ZIP 중 하나라도 빠진 상태를 정상 릴리스 완료로 보고하지 않는다. 패치에 삭제·이동이 필요하면 자동 실행 스크립트 또는 명시적 삭제 목록을 포함한다.


## v1.11.1 기기 보정·전투 모션 고정 규칙

- DeviceCalibration은 시각 품질과 회복 대기만 조절하며 공격 판정·피해·AI·쿨다운을 변경하지 않는다.
- 브라우저 메모리·코어·DPR 힌트를 실제 온도·배터리·GPU 메모리 측정값으로 보고하지 않는다.
- PlayerMotionDirector는 기존 8방향 Atlas 키와 공통 발 피벗을 유지한다.
- 보스 텔레그래프는 색상 외에 기호·패턴명·예고 단계와 시간을 함께 제공한다.
- 전투 버튼은 pointerId 하나만 소유하고 pointercancel 이후 상태가 남지 않아야 한다.
- v1.11.1 모션 블록아웃은 최종 런타임 원화로 보고하지 않는다.

## v1.11.2 선택형 전용 Atlas·실기기 QA 세션 고정 규칙

- LUMERIFT 소유 기하형 플레이어 Atlas는 `owned-preview` 선택형 검수 경로이며 기본 고급 원화를 자동 교체하지 않는다.
- 전용 플레이어 Atlas는 `player-owned-preview` Lazy Loading 번들에만 포함하고 `core-ui`와 기본 전투 번들의 초기 다운로드에 넣지 않는다.
- 전용 Atlas 로딩 실패 시 기존 고급 플레이어 Atlas로 자동 복구한다.
- 실제 방향 프레임을 사용하는 전용 Atlas에는 서쪽 방향 추가 미러링을 적용하지 않는다.
- 실기기 QA 세션은 3초 간격 프레임·품질·뷰포트 표본을 기록하되 브라우저가 제공하지 않는 표면 온도·GPU 메모리를 추정값으로 채우지 않는다.
- Battery Status API 값은 지원 브라우저에서만 참고값으로 기록하며 물리 계측을 대체하지 않는다.


## v1.11.4 QA 분석·도색 후보·전투 접근성 고정 규칙

- `owned-painted`는 선택형 제작 후보이며 기존 고급 기본 플레이어 Atlas를 자동 교체하지 않는다.
- 도색 후보는 별도 `player-owned-painted` Lazy Loading 번들에만 포함하고 초기 core/battle 번들에 넣지 않는다.
- QA 자동 분석은 프레임 표본 기반 권장값이며 실제 표면 온도·GPU 메모리·물리 배터리 측정을 대체하지 않는다.
- 진동과 화면 낭독은 각각 끌 수 있어야 하고, 미지원 브라우저에서 예외가 전투 루프를 중단하지 않아야 한다.
- 화면 낭독은 중복 메시지를 억제하고 위험·정밀 회피·저체력 등 우선순위를 구분한다.
- 다층 음향은 기존 OGG를 재사용하는 프레젠테이션 계층이며 독립 수작업 음향 완성으로 보고하지 않는다.


## v1.11.4 입력 보정·UI 정리·캐릭터 polish 고정 규칙
- 가상 조이스틱은 기본값을 `반전 보정(reverse)`으로 시작하며 설정 화면에서 `화면 기준/좌우 반전/상하 반전`까지 순환 전환할 수 있다.
- 전투 이동 보정은 터치 가상 조이스틱에만 적용하고 키보드와 포인터 타깃 이동은 기존 규칙을 유지한다.
- 플레이어 기본 전투 표현은 그림자·발밑 링·실루엣 glow를 유지해 캐릭터의 방향성과 전투 가독성을 높인다.
- UI는 기존 glass 감성을 유지하되 패널 대비와 포인트 컬러를 높여 칙칙함과 난잡함을 줄인다.


## v1.11.5 웹툰형 UI·전투 HUD·타격감 고정 규칙
- 웹툰형 UI 리디자인은 기존 런타임 자산과 Graphics 기반 장식으로 구현하며 신규 대용량 아트 번들을 기본 다운로드에 추가하지 않는다.
- ATTACK VECTOR 패널과 조이스틱 방향 가이드는 검수 보조이며 캐릭터 이동/공격 판정 로직을 바꾸지 않는다.
- CRIT/BURN/HIT 플로팅 텍스트와 CHAIN/CRITICAL 배너는 표현 계층이며 피해 공식·확률·보스 AI를 변경하지 않는다.
- 전투 HUD는 상단 카드, 중앙 피드백, 하단 입력 도크 구조를 유지해 한 손 플레이와 9:16 모바일 가독성을 우선한다.
- 버튼 배지와 패널 강조는 시각 보강 수단이며 접근성 대비·저시력 환경에서 정보 손실을 유발하지 않아야 한다.


## v1.11.6 8방향 이동 그래픽 강화 고정 규칙
- 8방향 이동 스무딩과 방향 리본·발자국 하이라이트는 표현 계층이며 이동 입력 처리·충돌·공격 판정 로직을 변경하지 않는다.
- 새로운 이동 polish는 기존 8방향 Atlas 키와 플레이어 발 피벗을 유지해야 하며, 방향 전환 지연이 입력 체감 저하로 이어지지 않아야 한다.
- 그래픽 디테일 강화는 Graphics 중심으로 구현하고 기본 다운로드에 대용량 신규 아트 번들을 추가하지 않는다.
- 이동 가독성 강화 요소는 저시력 환경과 reduced motion 설정에서 정보 손실이나 과도한 시각 피로를 유발하지 않아야 한다.
- 8방향 공격 성공 프레임과 타격 포즈 검수는 다음 단계이며, 아직 최종 전용 원화 완성으로 보고하지 않는다.


## v1.11.7 자동 타겟·자동 전투·전체 UI 리뉴얼 고정 규칙
- 자동 타겟은 기본 ON, 자동 전투는 기본 OFF로 유지한다.
- 자동 전투가 켜지면 자동 타겟을 함께 켜되, 자동 타겟 OFF 시 자동 전투도 함께 꺼진다.
- 수동 조이스틱·키보드·직접 버튼 입력은 자동 이동과 자동 행동보다 항상 우선한다.
- 자동 전투는 표현·입력 보조 계층이며 충돌·피해·치명타·드롭·보스 AI·랭킹 점수를 변경하지 않는다.
- 튜토리얼 중에는 자동 전투가 개입하지 않는다.
- 첫 시작 UI 리뉴얼은 Boot·Login·Lobby·공통 SceneChrome·Battle까지 같은 시각 언어를 유지한다.
- 공통 인터페이스 리뉴얼을 위해 신규 대용량 런타임 이미지를 기본 번들에 추가하지 않는다.


## v1.11.8 초대규모 UI·자동 세부 설정 고정 규칙
- RIFT INTERFACE 장식은 Graphics 기반 프레젠테이션 계층이며 게임 판정과 저장 데이터를 변경하지 않는다.
- 자동 타겟 우선순위와 실기기 프리셋은 타겟 점수·전환 안정성·자동 행동 간격에만 영향을 준다.
- 자동 스킬·자동 회피·보스전 제한은 사용자가 개별 제어할 수 있어야 한다.
- 수동 입력이 자동 행동보다 항상 우선하며 튜토리얼에서 자동 전투를 금지한다.
- 자동 보조 설정은 QA JSON에 기록하되 실제 장시간 기기 성능 측정을 대체하지 않는다.


## v1.11.9 자동 진단·Safe Area·방향 공격 포즈 고정 규칙
- 자동 타겟 점수와 자동 행동 이유는 설명용 표현 계층이며 실제 피해·판정·AI 우선순위를 숨겨 변경하지 않는다.
- 수동 입력은 자동 전투보다 항상 우선하며 자동 복귀 지연이 수동 입력을 가로채지 않아야 한다.
- HP 조건형 자동 스킬과 보스 회피 정책은 사용자 설정에만 따라 작동하며 기본값은 보수적으로 유지한다.
- Safe Area 보정은 버튼 위치·스케일만 조정하고 전투 월드 좌표와 판정 좌표를 변경하지 않는다.
- 방향별 공격 포즈는 Atlas 키와 AttackFootprint를 유지하는 표현 계층이며 최종 신규 원화 완성으로 보고하지 않는다.


## v1.11.18 캐릭터·장비 외형 동기화 고정 규칙

- 캐릭터 FX Atlas는 기존 플레이어 본체·방향 키·발 피벗 위에 합성하는 표현 계층이며 이동·피해·충돌·AI·쿨다운을 변경하지 않는다.
- 장비 외형은 현재 장착 정보에서 매번 계산하며 Player Save v4에 외형 전용 필드를 추가하지 않는다.
- 장비 등급 색상은 전투 가독성을 보조하되 등급·능력치·보상 판정 자체를 변경하지 않는다.
- 로비·인벤토리 미리보기와 전투 재질은 동일한 `CharacterEquipmentAppearance` 계약을 사용한다.
- 신규 이미지 자산은 PNG/WebP만 사용하고 `ASSET_MANIFEST.json`, Asset Registry, 초기 15MB 예산을 함께 갱신한다.
- 프로그램 생성 보조 FX와 재질 오버레이를 전용 수작업 캐릭터 본체 Atlas 완성으로 과장하지 않는다.


## v1.11.19 프리미엄 본체·염색 고정 규칙

- 기본 전투 플레이어는 `player_premium_body_v10`을 사용하며 `player.<state>.<direction>` 키를 유지한다.
- 레거시 v4 플레이어 Atlas는 fallback·라이선스 근거로 보존하되 기본 전투에서 중복 로드하지 않는다.
- 염색 프리셋은 로컬 표시 설정이며 Player Save v4 스키마를 변경하지 않는다.
- 무기 실루엣은 장착 장비에서 계산하며 공격 판정과 `AttackFootprint`를 변경하지 않는다.
- 본 Atlas는 production-candidate 파생 자산이며 물리 기기 검수 전 최종 수작업 원화로 표현하지 않는다.


## v1.11.20 캐릭터 아틀리에·무기 모션 고정 규칙

- 외형 슬롯은 로컬 표시 설정이며 Player Save v4·장비 성장·보상 데이터를 변경하지 않는다.
- 아틀리에 미리보기는 실제 v10 Atlas 애니메이션 키를 사용하고 존재하지 않는 완성 자산을 주장하지 않는다.
- 무기 모션 프로필은 전투 진입 시 불변 복사본에만 적용하며 원본 JSON 액션 데이터를 변조하지 않는다.
- 공격 범위·피격 판정은 조정된 액션 데이터를 사용하는 기존 AttackFootprint 단일 계산을 유지한다.
- 물리 Android·iOS 기기 검수 전 크기·발광·입력 체감을 최종 승인으로 기록하지 않는다.

## v1.11.21 캐릭터 스튜디오·무기 프레임·모바일 보정 고정 규칙

- 8방향 수동 회전은 실제 `player.<pose>.<direction>` Atlas 키를 사용하고 서쪽 방향을 임의로 중복 미러링하지 않는다.
- 장비 교체 전후 비교는 임시 `equipmentOverrides`로 계산하며 Player Save v4·인벤토리 UID·장착 데이터를 수정하지 않는다.
- 코스튬 세트와 세부 염색 채널은 로컬 표시 설정이며 전투 능력치·보상·랭킹 점수에 영향을 주지 않는다.
- 검·대검·장창 본체 공격 프레임은 기존 v10 Atlas 재구성 단계이며 신규 수작업 전용 Atlas 완성으로 보고하지 않는다.
- 모션 타이밍은 원본 행동 JSON을 변조하지 않고 전투 진입 시 불변 복사본에 적용한다.
- 공격 텔레그래프와 피격 판정은 계속 `AttackFootprint` 단일 기하 계산을 사용한다.
- Android Chrome·iOS Safari 크기·발광 프로필은 실제 캡처 근거가 확보되기 전 `pending-physical-capture`로 유지한다.

## v1.11.22 무기 공격 Atlas·외형 프리셋 고정 규칙

- `player_weapon_attack_body_v11`은 v10 본체 파생 production-candidate이며 최종 수작업 원화로 보고하지 않는다.
- 전용 공격 Atlas 누락·파싱 실패·대체 플레이어 원화 선택 시 기존 v10 프레임 레시피로 안전하게 폴백한다.
- 파트 집중 보기와 확대는 캐릭터 스튜디오 표현 계층이며 장비 UID·능력치·AttackFootprint를 변경하지 않는다.
- 외형 프리셋 JSON은 Player Save v4와 분리하고 스키마·게임·종류가 일치하는 파일만 가져온다.
- 즐겨찾기·이름 변경은 로컬 외형 메타데이터이며 Cloud Save·랭킹·보상에 영향을 주지 않는다.
- 물리 Android/iOS 캡처가 없으면 크기·발광 보정을 `capture-verified`로 전환하지 않는다.

## v1.11.23 독립 장비 레이어·캡처 승인 고정 규칙

- 갑주·망토·룬 프로그램 레이어를 아이템별 수작업 최종 원화로 보고하지 않는다.
- `capture-verified`는 승인 조건을 충족한 로컬 JSON이 있는 해당 플랫폼에만 적용한다.
- 프로젝트 자체에서 물리 단말 캡처를 수행하지 않은 상태를 완료로 기록하지 않는다.
- 외형 프리셋 Archive v2는 v1 가져오기 하위 호환을 유지한다.
- 고정 슬롯은 수동 덮어쓰기와 JSON 가져오기 병합에서 로컬 값을 우선한다.
- 외형 Cloud Save는 사용자 명시 동의 전 자동 연결하지 않으며 App Check 비활성 결정을 유지한다.


## v1.11.24 외형 Cloud Save·아이템 마스크 고정 규칙

- 외형 Cloud Save는 사용자 명시 동의 전 원격 읽기·쓰기를 실행하지 않는다.
- 문서는 `users/{uid}/settings/characterAppearance` 한 곳만 사용하고 UID 불일치를 거부한다.
- 로컬·원격이 모두 변경되면 자동 덮어쓰지 않고 충돌 상태로 중지한다.
- Cloud 가져오기 시 고정 슬롯은 로컬 값을 보존하고 병합된 통합본을 다시 업로드한다.
- Archive v3는 슬롯 순서를 포함하며 v1·v2 가져오기 하위 호환을 유지한다.
- 아이템별 장비 마스크와 무기 프레임 정렬은 표현 계층이며 Player Save v4·AttackFootprint를 변경하지 않는다.
- App Check는 비활성 상태를 유지하며 실제 물리 기기 캡처가 없으면 `capture-verified` 전역 승인을 주장하지 않는다.

## v1.11.25 외형 충돌 선택 병합·복구 고정 규칙

- 로컬·Cloud 외형이 모두 변경되면 전체 자동 덮어쓰기를 금지하고 6개 범주별 선택 병합을 제공한다.
- 로컬에서 고정한 외형 슬롯 내용은 사용자가 Cloud 전체 선택을 눌러도 덮어쓰지 않는다.
- 충돌 병합과 Cloud 업로드 전 현재 로컬 Archive를 복구 지점으로 보존한다.
- 복구 적용 전 현재 상태를 다시 백업하고 복구 직후 Cloud 자동 업로드를 금지한다.
- 외형 복구 JSON은 현재 로그인 UID와 일치할 때만 가져온다.
- 외형 복구는 Player Save v4·장비 성장·보상·랭킹 데이터와 분리한다.
- 텍스트 기반 충돌 요약을 실제 좌우 캐릭터 이미지 비교 완료로 보고하지 않는다.
- 실제 물리 단말 캡처와 수작업 공격·장비 원화가 없으면 완료로 기록하지 않는다.
